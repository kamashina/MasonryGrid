# Masonry Grid / Waterfall Layout (React Native + Expo + TypeScript)

Проект: UI-kit мобильного приложения
Задача: Реализовать компонент **Masonry Grid / Waterfall Layout**

---

## 📦 Установка и запуск

### 1. Установить зависимости

```bash
npm install
```

### 2. Запустить проект

```bash
npx expo start
```

После запуска вы увидите QR-код и меню Expo Dev Tools.

---

## 📱 Запуск через Expo Go

### iOS

1. Установите приложение **Expo Go** из App Store
2. Запустите команду:

```bash
npx expo start
```

3. Откройте камеру iPhone → наведите на QR-код → нажмите уведомление
4. Проект откроется в Expo Go

> Телефон и компьютер должны быть в одной Wi-Fi сети.

---

### Android

1. Установите **Expo Go** из Google Play
2. Выполните:

```bash
npx expo start
```

3. Отсканируйте QR-код в Expo Go

---

## 🧠 Архитектура Masonry Layout

### 🎯 Главная идея

Сетка должна:

- поддерживать **динамическую высоту карточек**
- раскладывать карточки по принципу **минимальной суммарной высоты колонки**
- не зависеть от типа данных (универсальный компонент)
- корректно перестраиваться при добавлении новых элементов

---

## 🧱 Компоненты

### ### 1. `MasonryGrid`

Универсальный компонент, который:

- принимает массив данных `data`
- измеряет высоту каждой карточки через `onLayout`
- сохраняет высоты в `Record<number, number>`
- вызывает хук раскладки `useMasonryColumns`
- рендерит итоговые колонки

```tsx
<MasonryGrid
  data={data}
  renderItem={(item, width) => <MasonryCard item={item} width={width} />}
/>
```

---

### 2. `useMasonryColumns` — логика раскладки

Хук реализует алгоритм Waterfall:

1. Для каждого элемента смотрим измеренную высоту (`heights[index]`)
2. Находим колонку с минимальной суммарной высотой
3. Кладём элемент в эту колонку
4. Обновляем её высоту
5. Возвращаем итоговый массив колонок

---

### 3. `MasonryCard`

Пример карточки для отображения:

- можно заменить на любой компонент

---

## ⚠ Известные ограничения

- Используется `ScrollView` вместо `FlatList`  
  Для небольших списков норм, но при больших массивах данных нужна виртуализация.

---

## 💡 Возможные улучшения

- Виртуализация Masonry через `FlatList`
- Анимации перестройки (Reanimated)
